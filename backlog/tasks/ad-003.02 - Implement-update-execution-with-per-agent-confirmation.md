---
id: AD-003.02
title: Implement update execution with per-agent confirmation
status: Done
assignee:
  - claude
created_date: '2026-03-22 20:58'
updated_date: '2026-03-22 21:17'
labels: []
dependencies:
  - AD-003.01
parent_task_id: AD-003
priority: medium
---

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 agentdots update runs update commands only for agents that have an available update
- [x] #2 Without -y flag: prompts user to confirm each update command before executing it
- [x] #3 With -y flag: executes all available update commands without prompting
- [x] #4 Shows exit status / success or failure for each executed update command
- [x] #5 Agents without an updateCommand or with unknown latest version are skipped with a note
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Add runCommand(cmd) utility to src/updates/runner.ts — executes a shell command, streams output, returns success/failure
2. Add promptConfirm(question) utility to src/updates/prompt.ts — reads a y/n from stdin
3. Add executeUpdates(results, opts) to src/updates/executor.ts — orchestrates: filter pending, confirm or skip, run, report
4. Wire into CLI: replace stub in update command action with executeUpdates call
5. Tests in test/updates/executor.test.ts covering: -y skips prompts, confirmation yes runs, confirmation no skips, skips agents without updateCommand
<!-- SECTION:PLAN:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added src/updates/runner.ts (shell exec), src/updates/prompt.ts (y/n stdin), src/updates/executor.ts (orchestration with injected deps for testability). Wired into CLI update command: shows table, then runs confirmed updates, reports success/failure/skipped. 7 new executor tests. All 71 tests pass.
<!-- SECTION:FINAL_SUMMARY:END -->
